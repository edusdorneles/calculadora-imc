import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    containerFooter: {
        position: 'absolute',
        left: 0,
        right: 0,
        bottom: 0,
        padding: 10,
    },
    
    footerText: {
        textAlign: 'center',
    },
});

export default styles;