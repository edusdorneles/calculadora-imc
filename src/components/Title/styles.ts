import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  titleContainer: {
    padding: 50,
    alignItems: "center",
  },

  title: {
    fontSize: 30,
    fontWeight: "bold",
    color: "#FFFFFF",
  },
});
